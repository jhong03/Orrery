# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# NOTE: public/textures/ used to be ignored (fetched via scripts/fetch-assets.mjs),
# but the textures are now committed — Solar System Scope blocks CI/datacenter
# IPs, so a fetch-at-build step is unreliable for deploys.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Electron build output
release

# Dev verification artifacts
scripts/tour/
scripts/smoke-*.png
